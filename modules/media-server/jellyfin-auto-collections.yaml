crontab: !ENV ${CRONTAB}
timezone: !ENV ${TZ}
jellyfin:
  server_url: !ENV ${JELLYFIN_SERVER_URL}
  api_key: !ENV ${JELLYFIN_API_KEY}
  user_id: !ENV ${JELLYFIN_USER_ID}
plugins:
  letterboxd:
    enabled: true
    list_ids:
      - arinbicer/list/mcu
